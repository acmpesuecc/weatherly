import React from 'react'

/*
    This is the search bar component, it would allow you to make searches from a particular datasource and display results.
*/


const SearchBar = () => {
    return (
        <div>
            this is search bar
        </div>
    )
}
export default SearchBar;
